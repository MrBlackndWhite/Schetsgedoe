using System;
using System.Windows.Forms;

namespace plswork
{
    static class Program
    {
        [STAThread]
        static void Main()
        {
            Application.Run(new Hoofdscherm());
        }
    }
}
